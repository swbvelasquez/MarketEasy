<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Los items tienen que tener el mismo id del frament en el nav_graph para que funcione el setupwithnavcontroller-->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:title="@string/home_title"
            android:icon="@drawable/ic_home"/>
        <item
            android:id="@+id/nav_profile"
            android:title="@string/profile_title"
            android:icon="@drawable/ic_profile" />
        <item
            android:id="@+id/nav_catalogue"
            android:title="@string/catalogue_title"
            android:icon="@drawable/ic_catalogue" />
    </group>
</menu>